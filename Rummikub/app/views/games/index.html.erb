<h1>Rummikub!</h1>

<ol>
	<% @games.each do |game| %>
		<li>
			<%= link_to heading(game), game %>
		</li>
	<% end %>
</ol>

<%= form_for Game.new do |f| %>
	<%= f.submit "Create new game!" %>
<% end %>

